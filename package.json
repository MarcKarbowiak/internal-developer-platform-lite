{
  "name": "internal-developer-platform-lite",
  "version": "0.1.0",
  "private": true,
  "description": "CLI-first Internal Developer Platform reference focused on golden-path service scaffolding.",
  "bin": {
    "idp-lite": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "create": "node dist/cli.js create"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3"
  }
}
